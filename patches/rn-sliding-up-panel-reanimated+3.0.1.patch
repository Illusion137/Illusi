diff --git a/node_modules/rn-sliding-up-panel-reanimated/SlidingUpPanel.js b/node_modules/rn-sliding-up-panel-reanimated/SlidingUpPanel.js
index ddaf669..184c4ec 100644
--- a/node_modules/rn-sliding-up-panel-reanimated/SlidingUpPanel.js
+++ b/node_modules/rn-sliding-up-panel-reanimated/SlidingUpPanel.js
@@ -101,6 +101,7 @@ const SlidingUpPanel = forwardRef(function SlidingUpPanel(props, ref) {
     onMomentumDragEnd = () => {},
     allowMomentum = true,
     allowDragging = true,
+    blockingGesture = null,
     showBackdrop = true,
     backdropOpacity: backdropOpacityProp = 0.75,
     friction = Constants.DEFAULT_FRICTION,
@@ -287,13 +288,23 @@ const SlidingUpPanel = forwardRef(function SlidingUpPanel(props, ref) {
     return () => sub.remove()
   }, [handleBackButton])
 
+  // Whether the pan actually activated (the finger moved past minDistance).
+  // onBegin/onFinalize fire for EVERY touch inside the panel — including plain
+  // taps on buttons — so any position work done there corrupts in-flight
+  // show()/hide() springs (tap during the opening spring used to cancel it and
+  // snap the panel back to the bottom).
+  const dragActivated = useSharedValue(false)
+
   // ─── pan gesture (runs on the UI thread via RNGH) ────────────────────────
   const panGesture = Gesture.Pan()
     .enabled(allowDragging)
     .minDistance(minimumDistanceThreshold)
-    .onBegin(() => {
+    .onStart(e => {
+      dragActivated.value = true
       cancelAnimation(position)
-      initialDragPosition.value = position.value
+      // Compensate for the distance already travelled before activation
+      // (minDistance) so the panel doesn't jump on drag start.
+      initialDragPosition.value = position.value + e.translationY
       runOnJS(onDragStart)(position.value)
     })
     .onUpdate(e => {
@@ -342,12 +353,29 @@ const SlidingUpPanel = forwardRef(function SlidingUpPanel(props, ref) {
       }
     })
     .onFinalize((_, success) => {
-      // Covers gesture cancellation / termination (no onEnd fires in those cases)
-      if (!success) {
-        runOnJS(onDragEnd)(position.value)
-      }
+      const wasDrag = dragActivated.value
+      dragActivated.value = false
+      // A successful gesture already resolved its rest position in onEnd, and a
+      // touch that never activated (a simple tap) must not move the panel at all.
+      if (success || !wasDrag) return
+      // An activated drag was cancelled / terminated (no onEnd fires then), so
+      // the panel would be stranded at a partial position. Snap to the nearest
+      // rest point so it never gets stuck half-open with a stale header.
+      runOnJS(onDragEnd)(position.value)
+      const allPoints =
+        snappingPoints.length > 0 ? [bottom, ...snappingPoints, top] : [bottom, top]
+      const snapTarget = closestPoint(position.value, allPoints)
+      position.value = withTiming(snapTarget, {duration: 300})
     })
 
+  // A gesture (e.g. a child ScrollView's native scroll) that must fail before
+  // the panel pan may activate. Lets callers keep the panel draggable everywhere
+  // EXCEPT over that child: touches on the child go to its own gesture, touches
+  // elsewhere never reach it (it fails immediately) so the pan works as normal.
+  if (blockingGesture) {
+    panGesture.requireExternalGestureToFail(blockingGesture)
+  }
+
   // ─── render ───────────────────────────────────────────────────────────────
   const renderBackdrop = () => {
     if (!showBackdrop) return null
@@ -427,6 +455,7 @@ SlidingUpPanel.propTypes = {
   onBottomReached: PropTypes.func,
   allowMomentum: PropTypes.bool,
   allowDragging: PropTypes.bool,
+  blockingGesture: PropTypes.object,
   showBackdrop: PropTypes.bool,
   backdropOpacity: PropTypes.number,
   friction: PropTypes.number,
diff --git a/node_modules/rn-sliding-up-panel-reanimated/index.d.ts b/node_modules/rn-sliding-up-panel-reanimated/index.d.ts
index 86ccd80..638ec77 100644
--- a/node_modules/rn-sliding-up-panel-reanimated/index.d.ts
+++ b/node_modules/rn-sliding-up-panel-reanimated/index.d.ts
@@ -1,6 +1,6 @@
 import React, {ReactElement} from 'react'
 import {SharedValue} from 'react-native-reanimated'
-import {PanGesture} from 'react-native-gesture-handler'
+import {PanGesture, GestureType} from 'react-native-gesture-handler'
 
 // Inline the two react-native style types we need so react-native doesn't
 // have to be installed as a devDependency of this library.
@@ -29,6 +29,12 @@ export interface SlidingUpPanelProps {
   onBottomReached?: () => any
   allowMomentum?: boolean
   allowDragging?: boolean
+  /**
+   * A gesture (e.g. a child ScrollView's Gesture.Native()) that must fail
+   * before the panel pan may activate. Keeps the panel draggable everywhere
+   * except over that child.
+   */
+  blockingGesture?: GestureType
   showBackdrop?: boolean
   backdropOpacity?: number
   friction?: number
